Step 1: Install Required Tools
1. Install Terraform
Download from: Terraform
Link: https://developer.hashicorp.com/terraform/downloads

Steps (Windows):

Download ZIP
Extract it
Add path to Environment Variables
Verify:
terraform -v
2. Install AWS CLI
Download from: AWS CLI
Link: https://aws.amazon.com/cli/

Verify:

aws --version
Step 2: Create AWS Account & Credentials

Go to Amazon Web Services

Create IAM User
Open AWS Console
Go to IAM → Users → Create User
Enable:
Programmatic access
Attach policy:
AdministratorAccess (for testing)
Save:
Access Key
Secret Key
Step 3: Configure AWS CLI

Run:

aws configure

Enter:

Access Key
Secret Key
Region (e.g., us-east-1)
Output format (json)
Step 4: Create Terraform Project Folder

Create a folder:

mkdir terraform-ec2
cd terraform-ec2
Step 5: Create Required Files

Create these 4 files:

main.tf
variables.tf
terraform.tfvars
outputs.tf
Step 6: Write Code
1. main.tf
provider "aws" {
  region = var.aws_region
}

resource "aws_instance" "my_ec2" {
  ami           = var.ami_id
  instance_type = var.instance_type
  key_name      = var.key_name

  tags = {
    Name = "Terraform-EC2"
  }
}
2. variables.tf
variable "aws_region" {
  description = "AWS region"
  type        = string
}

variable "ami_id" {
  description = "AMI ID"
  type        = string
}

variable "instance_type" {
  description = "EC2 instance type"
  type        = string
}

variable "key_name" {
  description = "Key pair name"
  type        = string
}
3. terraform.tfvars

IMPORTANT: Replace values correctly

aws_region     = "us-east-1"
ami_id         = "ami-0c55b159cbfafe1f0"   # Amazon Linux (example)
instance_type  = "t2.micro"
key_name       = "your-key-name"
4. outputs.tf
output "instance_id" {
  value = aws_instance.my_ec2.id
}

output "public_ip" {
  value = aws_instance.my_ec2.public_ip
}
Step 7: Create Key Pair in AWS
Go to EC2 Dashboard
Click Key Pairs
Create new key
Download .pem file
Use that name in terraform.tfvars
Step 8: Run Terraform Commands
Initialize
terraform init
Plan
terraform plan

You will see preview of EC2 creation

Apply
terraform apply

Type:

yes
Step 9: Verify EC2 Instance

Go to:

EC2 Dashboard → Instances
You should see your instance running
Step 10: Output Example

After apply, you will see:

instance_id = i-xxxxxxxxxxxx
public_ip   = xx.xx.xx.xx
Step 11: (IMPORTANT) Destroy Resources

To avoid AWS charges:

terraform destroy
